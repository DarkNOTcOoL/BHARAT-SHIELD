# Root-level requirements.txt for Vercel's Python runtime.
# Vercel installs dependencies from this file before invoking api/index.py.
# Keep in sync with backend/requirements.txt.
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
sqlalchemy>=2.0.40,<3
python-multipart>=0.0.20,<1
pydantic>=2.11,<3
